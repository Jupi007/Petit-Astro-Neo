{% set formContent %}
	{{ form_start(_parent.registrationForm, {attr: {class: 'max-w-lg mx-auto'}}) }}
	{{ form_end(_parent.registrationForm) }}
{% endset %}

{% include "includes/common/_page_container.html.twig" with {
	'content': formContent,
	'class': 'my-4'
} only %}
