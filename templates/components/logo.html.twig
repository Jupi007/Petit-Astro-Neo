<a
  href="{{ sulu_content_root_path() }}"
  {{ attributes.defaults({
    class: 'block drop-shadow-vertical'
  }) }}
>
  <img
    src="{{ asset('build/website/images/logo.svg') }}"
    alt="Logo"
    loading="lazy"
  />
</a>
